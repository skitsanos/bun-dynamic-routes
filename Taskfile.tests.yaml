version: 3
silent: true
tasks:
  default:
    desc: Run isolated server regressions; no existing server or upload cleanup required
    cmds:
      - bun test tests
  compiled:
    cmds:
      - bun run test:compiled
  docker:
    cmds:
      - bun run test:docker
  hurl:
    cmds:
      - bun run test:hurl
