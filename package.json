{
  "name": "bun-dynamic-routes",
  "version": "2.2.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "compile": "bun build --compile --production src/index.ts --outfile dist/demo",
    "lint": "bunx biome check src/",
    "lint:fix": "bunx biome check src/ --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.2 || ^6.0.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
